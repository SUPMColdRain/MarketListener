# Third-party notices

`lightweight-charts.standalone.production.js` is bundled for offline Android
rendering from the Lightweight Charts 4.2.3 distribution. It is not loaded
from a network resource at runtime.
