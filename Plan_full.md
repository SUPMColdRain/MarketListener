# 全周期开发、审查与验收执行规范

## 1. 文档地位与交付目标

本文件是正式项目开发的完整任务计划。`STATUS.md` 是唯一实时状态源，`START_HERE.md` 是每个新任务的固定入口。发生冲突时按以下顺序处理：

1. `ADR.md` 与单项 ADR；
2. `STATUS.md` 中当前任务的状态、依赖和边界；
3. 本文件的任务定义与验收标准；
4. `行情监控和产业链图谱项目.md` 的产品与架构说明；
5. `Plan.md` 与 `docs/deliveries/D0-*` 仅作为 Day 0 历史档案。

正式开发先完成计划固化，再从 `STATUS.md` 中第一项 `READY` 任务逐项推进。不得自动续跑原 `Plan.md` 的 `D0-*`，不得改写 Day 0 失败记录。

## 2. 状态机与角色分离

正常状态固定为：

```text
PENDING → READY → IN_PROGRESS → REVIEW → ACCEPTANCE → ACCEPTED
```

异常状态：

- `CHANGES_REQUIRED`：审查或验收不通过，退回同一任务修复；修复完成后重新进入 `REVIEW`。
- `BLOCKED`：缺少凭据、授权、设备或外部条件；必须记录阻塞证据和解除条件。
- `CANCELLED`：仅用户可以取消。

状态规则：

- 只有全部依赖均为 `ACCEPTED`，任务才能进入 `READY`。
- 实现任务从 `READY` 进入 `IN_PROGRESS`，完成代码、自动化测试和交付记录后只能进入 `REVIEW`。
- 实现 Agent 不得审查或验收自己的差异，也不得把自己的任务标为 `ACCEPTED`。
- 审查任务只检查当前任务差异，结论为 `ACCEPTANCE` 或 `CHANGES_REQUIRED`。
- 验收任务重新运行命令和真实操作，只有验收角色可以在证据充分时将任务标为 `ACCEPTED`。
- 状态变化必须同时更新 `STATUS.md` 的更新时间、证据链接、阻塞项和下一项任务；其他文档不得维护平行状态表。

每个 `FULL-*` 任务对应三个独立任务/会话：

1. 实现：编码、测试、交付记录，结束于 `REVIEW`。
2. 审查：按严重度输出带文件和行号的问题，结束于 `ACCEPTANCE` 或 `CHANGES_REQUIRED`。
3. 验收：重跑自动化命令和必要的真实操作，决定是否进入 `ACCEPTED`。

## 3. 全局约束与完成定义

- 仅支持 Android 13+，发布必须满足 16 KB 页面设备要求。
- 数据生产端使用 Python 3.11；Android 构建基线使用 JDK 21、锁定版本的 Gradle/AGP/Kotlin/SDK。
- Android 使用声明式策略 DSL，不执行 Python，不等待 Chaquopy + NumPy。
- 离线电脑更新与签名行情包是正式版主路径；QMT 和云端是门控增强，不阻塞离线正式版。
- 凭据只由用户在本机安全配置；仓库、APK、行情包、交付报告和日志不得包含凭据、私钥或个人数据。
- 数据源能力必须逐项真实探测；失败、缺权限和不支持不得伪装为成功或零值。
- 行情分区不得静默逐行混源；个人数据与行情数据物理隔离。
- 所有持续付费数据和云资源合计每年不得超过 2000 元，新增持续成本须用户单独批准。

任务只有在依赖满足、实现完成、自动化测试通过、独立审查通过、真实验收完成、迁移与安全影响已说明且证据写入交付记录后，才可进入 `ACCEPTED`。

## 4. 总依赖关系

```text
FULL-001 → FULL-002 → FULL-003
                         ↓
              FULL-100 → FULL-101
                         ↓
       FULL-110 || FULL-111 || FULL-112 || FULL-113
                         ↓
              FULL-120 → FULL-121 → FULL-122 → FULL-123
                                      ↓
                   FULL-200 || FULL-201 || FULL-300 || FULL-400 || FULL-500
                         ↓
              FULL-202 → FULL-203 → FULL-204
                         ↓
              Android、策略、复盘、市场扩展、图谱
                         ↓
              运维、全量回归、正式发布
```

`||` 表示依赖满足后可由不同任务并行执行。`FULL-610`、`FULL-804` 及候选商业 API 是外部门控支线，不阻塞离线正式版。

## 5. 任务清单

### 5.1 治理与工程基线

| 任务 | 开发步骤 | 依赖 | 验收标准 |
|---|---|---|---|
| FULL-001 | 创建本计划、状态表、启动协议、ADR-0007/0008并同步入口文档和时序图 | 无 | 文档无冲突；验收接受后 `FULL-002` 成为唯一首项 `READY` 开发任务 |
| FULL-002 | 建立首个 Git 基线，锁定 Python 3.11、JDK 21、Gradle、SDK和依赖版本 | 001 | 仓库有可回退提交；无凭据和大行情文件 |
| FULL-003 | 建立 `scripts/verify.ps1`，接入 pytest、Schema、Ruff、Android Lint/JVM测试和APK构建 | 002 | 一条命令完成基线验证；任一子项失败返回非零状态 |

### 5.2 数据源能力与真实数据闭环

| 任务 | 开发步骤 | 依赖 | 验收标准 |
|---|---|---|---|
| FULL-100 | Provider Contract v2：参数化请求、能力注册、来源描述和独立能力状态 | 003 | Schema与Python模型一致；迁移测试通过 |
| FULL-101 | 本地凭据配置、日志脱敏、探针超时、重试和CLI退出码 | 100 | 缺配置为 `BLOCKED/CONFIGURATION`；探针失败返回非零 |
| FULL-110 | 修复AKShare适配器，快照、日历、资金等接口分别探测 | 101 | 已知快照与日历真实PASS；资金失败不抹掉其他结果 |
| FULL-111 | JQData真实登录、额度、日线、分钟线、ETF、指数和期货探针 | 101 | 报告真实行数、时间范围、周期、额度和失败原因 |
| FULL-112 | 新增Tushare日线、基础资料、财务、日历和分钟权限探针 | 101 | 每个接口独立报告权限、积分、频率和覆盖 |
| FULL-113 | 修复BaoStock字段和周期请求，验证网络、日线及复权 | 101 | 网络不可达时受控失败；可达时完成重叠样本对比 |
| FULL-120 | 来源烘焙测试和主备决策 | 110–113中至少三个 `ACCEPTED` | 一个历史主源、一个日线校验源、一个分钟能力真实通过 |
| FULL-121 | 标准标的、K线、日历、复权、公司行动和来源追踪契约升级 | 120 | 固定样本与跨语言Schema测试通过 |
| FULL-122 | 真实数据进入Bronze/Silver、质检、签名行情包 | 121 | 包内不存在模拟行情；来源、截止时间和质量可追溯 |
| FULL-123 | Android导入真实签名包并离线显示K线 | 122 | Android 13+、16 KB设备断网显示日线和分钟线 |

### 5.3 标的、更新与数据包

| 任务 | 开发步骤 | 依赖 | 验收标准 |
|---|---|---|---|
| FULL-200 | 标的主数据、A股/ETF范围规则和历史成员有效期 | 121 | 回测使用当时成员，不存在未来信息泄漏 |
| FULL-201 | 交易日历、交易时段、复权因子和公司行动 | 121 | 午休、停牌、除权和期货夜盘固定测试通过 |
| FULL-202 | 每日增量采集、游标、重试、幂等和断点恢复 | 200、201 | 重复运行不重复；中断后可恢复 |
| FULL-203 | 完整性、价格、成交量、时区及跨源差异规则 | 202 | 阻断问题进入隔离区，不静默逐行混源 |
| FULL-204 | 热数据包、冷分片、增量包、签名和回滚 | 203 | 正常、重复、篡改、截断和降级包均按预期处理 |

### 5.4 Android 行情体验

| 任务 | 开发步骤 | 依赖 | 验收标准 |
|---|---|---|---|
| FULL-300 | Android导航、数据库迁移、行情库与个人库边界 | 123 | 迁移和行情替换不影响个人数据 |
| FULL-301 | 自选、标的列表、详情和专业K线交互 | 204、300 | 真实包下查询、缩放、周期切换和截止时间正确 |
| FULL-302 | 市场概览、指标、筛选、异常和质量提示 | 301 | 缺失与失败不能显示为零或正常 |
| FULL-303 | 热缓存、冷分片加载、存储清理和性能优化 | 302 | 目标数据量下无明显卡顿，清理不删除个人数据 |

### 5.5 声明式策略 DSL

| 任务 | 开发步骤 | 依赖 | 验收标准 |
|---|---|---|---|
| FULL-400 | Strategy DSL v1 Schema、运算符、指标和安全边界 | 121 | 拒绝未知节点、任意代码、网络和文件访问 |
| FULL-401 | 桌面Python扫描/回测与DSL参考解释器 | 203、400 | 无未来函数；运行记录包含数据和策略版本 |
| FULL-402 | Kotlin离线DSL解释器 | 300、400 | 超时、无数据和计算错误不会导致App崩溃 |
| FULL-403 | 桌面与Android共享测试向量和一致性测试 | 401、402 | 离散结果一致；浮点误差阈值明确 |
| FULL-404 | 策略参数编辑、启停、运行历史和信号解释页面 | 301、403 | 用户可审查触发条件、理由和风险标签 |

### 5.6 交易记录与复盘

| 任务 | 开发步骤 | 依赖 | 验收标准 |
|---|---|---|---|
| FULL-500 | 交易、费用、资金、持仓和策略归属数据模型 | 300 | 迁移、约束、加密和CRUD测试通过 |
| FULL-501 | 交易录入、导入、修订和持仓计算 | 500 | 部分成交、费用、出入金和拆分样本正确 |
| FULL-502 | 净值、胜率、盈亏比、回撤、暴露和归因 | 501、203 | 使用固定账本样本验证全部统计 |
| FULL-503 | 个人数据加密备份、恢复和错误回滚 | 500 | 恢复前后逐表及统计一致；错误密码不破坏原库 |
| FULL-504 | 交易复盘、图表、筛选和备注界面 | 502、503 | 真机完成完整录入、复盘、备份和恢复流程 |

### 5.7 市场与指标扩展

| 任务 | 开发步骤 | 依赖 | 验收标准 |
|---|---|---|---|
| FULL-600 | 港股代表样本、日历、复权和市场范围 | 204 | 港股样本完成真实跨源闭环 |
| FULL-601 | 期货、商品指数、主力与连续合约语义 | 204 | 夜盘交易日、换月和连续规则有固定测试 |
| FULL-602 | 同花顺指数、ETF去重和重要市场指标 | 204 | 每项指标有口径、单位、频率、来源和截止时间 |
| FULL-610 | QMT账户开通后的实时行情适配 | 101、用户开通QMT | 模拟运行稳定；断线恢复；实时数据不污染历史层 |

### 5.8 产业链图谱

| 任务 | 开发步骤 | 依赖 | 验收标准 |
|---|---|---|---|
| FULL-700 | 图谱实体、关系、证据、置信度和人工确认模型 | 003 | 公司、产品、行业、上下游术语无歧义 |
| FULL-701 | 导入现有HTML、Excel、PDF和公告 | 700 | 原始文件、位置、时间和解析版本可追溯 |
| FULL-702 | 企业名称归一、实体消歧、关系抽取和重复合并 | 701 | 金标样本精度达标；低置信结果进入待确认 |
| FULL-703 | 人工审核、修订和审计记录 | 702、500 | 自动结果不能覆盖人工确认 |
| FULL-704 | Android产业链搜索、公司关系和来源查看 | 703、300 | 可从关系追溯到原始来源和确认状态 |

### 5.9 运维、回归与发布

| 任务 | 开发步骤 | 依赖 | 验收标准 |
|---|---|---|---|
| FULL-800 | Windows每晚调度、锁、重试、通知和恢复 | 202、204 | 连续运行、重复触发和断电恢复场景通过 |
| FULL-801 | 数据质量、运行历史、存储和来源健康看板 | 800 | 失败和陈旧分区可见且可定位 |
| FULL-802 | 密钥轮换、日志脱敏、备份演练和依赖审计 | 801 | 仓库/APK/日志无凭据；旧包轮换策略明确 |
| FULL-803 | 全模块回归、Release APK和用户验收清单 | 所有非门控必选任务 | 自动化、真机、数据、恢复和安全测试全部通过 |
| FULL-804 | 廉价云可行性与成本决策 | 本机连续20次任务成功 | 年持续成本不超过2000元并经用户单独批准 |
| FULL-900 | 正式版本封板 | 803 | 标签、APK、能力报告、质量报告、验收报告和已知缺口齐全 |

## 6. 任务修改边界与必跑验证

### 6.1 通用边界

- “允许修改范围”既是主要产物范围，也是禁止无关改动的边界；确需修改未列路径时，交付记录必须说明直接依赖关系，审查者可据此判定是否越界。
- `FULL-003` 接受后，所有代码任务必须先成功运行 `powershell -ExecutionPolicy Bypass -File scripts/verify.ps1`，再运行下表的专项验证。统一验证不能代替真实 Provider、真机、恢复、性能或安全操作。
- 测试夹具只能进入 `tests/fixtures/` 或模块测试目录；真实凭据、私钥、个人数据和大体积真实行情只能保存在仓库外。
- 数据库、Schema、行情包、DSL 包或公开 CLI 发生变化时，必须同时修改对应契约/迁移测试并在交付记录中说明兼容性。
- 文档任务用链接、任务编号、状态机、敏感信息和冲突规则检查替代业务构建；没有业务代码变化时不得伪称完成了业务回归。

### 6.2 每项任务的允许范围与专项验证

| 任务 | 允许修改范围 | 必跑专项验证/真实操作 |
|---|---|---|
| FULL-001 | 根治理文档、`docs/adr/`、`docs/deliveries/`、模块入口 README、Agent 时序图；禁止业务代码 | 任务编号/依赖/状态机集合检查，Markdown/HTML 本地链接与敏感信息检查，现行规则冲突搜索 |
| FULL-002 | Git 元数据、忽略/属性文件、Python/Android 工具链配置与锁文件、环境说明；禁止业务行为变化 | Python/JDK/Gradle/SDK 版本命令，依赖解析，凭据与大文件扫描，确认可回退首个提交 |
| FULL-003 | `scripts/verify.ps1`、Ruff/pytest/Gradle/Lint 配置及验证说明；业务代码只允许修复基线暴露的问题并单列 | 成功运行统一验证；人为制造一个受控失败证明非零退出后还原；记录各子命令结果 |
| FULL-100 | `contracts/provider-*`、Provider 基类/注册表/模型、共享夹具与对应测试 | Provider v1→v2 迁移、JSON Schema/Python 往返、未知能力与非法状态拒绝测试 |
| FULL-101 | 本地配置加载、日志脱敏、Provider runner、CLI、`.env.example` 和对应测试 | 缺配置、超时、重试、限流、脱敏、部分失败及 CLI 退出码测试；日志敏感信息扫描 |
| FULL-110 | AKShare 适配器、能力登记、固定响应夹具、专项测试和真实探针报告 | 单元/快照回归；快照、日历、资金接口分别真实探测并记录独立状态 |
| FULL-111 | JQData 适配器、能力登记、固定响应夹具、专项测试和真实探针报告 | 认证/额度/频率单元测试；真实登录后分别探测日线、分钟、ETF、指数、期货并记录行数与范围 |
| FULL-112 | Tushare 适配器、能力登记、固定响应夹具、专项测试和真实探针报告 | 权限/积分/限流单元测试；真实探测日线、基础资料、财务、日历和分钟权限 |
| FULL-113 | BaoStock 适配器、字段/周期映射、固定响应夹具、专项测试和真实探针报告 | 字段、周期、复权、网络超时测试；可达时真实日线及重叠样本对比 |
| FULL-120 | 能力路由/主备配置、烘焙比较逻辑、决策报告和对应测试；不得改 Provider 原始结果 | 分区级选源/降级/禁止逐行混源测试；复核至少三个已接受来源报告及三类真实能力门槛 |
| FULL-121 | 标的/K线/日历/复权/公司行动/来源追踪 Schema、两端模型、迁移与共享夹具 | Python Schema/模型测试与 Android 契约测试；旧版本迁移、时间/价格不变量和跨语言固定样本 |
| FULL-122 | Bronze/Silver、质检、行情包/签名生产代码、CLI、测试及仓库外真实运行产物 | 存储/质检/签名单元测试；从真实 Provider 运行到签名包并检查无模拟行情、来源与截止时间 |
| FULL-123 | Android 行情包导入、激活库读取、K线 UI/资源及对应 JVM/设备测试 | JVM 测试、Debug APK 与 16 KB 检查；Android 13+ 16 KB 设备断网导入真实包并显示日线/分钟线 |
| FULL-200 | 标的目录、范围定义/成员有效期、相关 Schema/存储/CLI 和测试 | 时间点成员查询、代码复用/退市/历史回测样本测试，明确验证无未来成员泄漏 |
| FULL-201 | 日历、交易时段、复权因子、公司行动、聚合逻辑与测试 | A股午休/停牌/除权、港股尾部、期货夜盘与交易日归属固定样本 |
| FULL-202 | 增量采集、游标、运行记录、锁、重试/恢复 CLI 与测试 | 首跑/重跑幂等、受控中断、断点续跑、部分来源失败和并发锁测试 |
| FULL-203 | 质量规则、跨源比较、隔离区、报告模型与测试 | 缺口/OHLC/成交量/时区/异常跳变/跨源阈值固定样本，验证阻断和不逐行混源 |
| FULL-204 | 热包/冷分片/增量包构建、签名、Android 导入/回滚协议及测试 | 正常、重复、篡改、截断、降级、空间不足和回滚测试；设备上验证个人库不变 |
| FULL-300 | Android 导航、Room/SQLCipher Schema与迁移、行情/个人库边界及测试 | JVM/Room 迁移与 CRUD 测试、APK 构建；设备上替换/删除行情库并确认个人数据保留 |
| FULL-301 | Android 自选、标的列表/详情、K线交互、ViewModel/查询层及 UI 测试 | JVM/Compose 测试；真实包设备操作查询、缩放、周期切换和截止时间显示 |
| FULL-302 | Android 市场概览、指标、筛选、异常/质量状态及查询/UI测试 | 缺失/失败/陈旧/部分数据状态测试；设备上确认不显示为零或正常 |
| FULL-303 | Android 热缓存、冷分片加载、清理策略、性能测试工具和说明 | 目标数据量基准、冷分片加载/卸载、低空间和清理回归；确认个人数据零删除 |
| FULL-400 | Strategy DSL Schema、Python模型/验证器、共享合法/非法夹具和规范 | Schema 正反例、未知节点、深度/规模限制、任意代码、网络/文件节点拒绝测试 |
| FULL-401 | 桌面 DSL 参考解释器、扫描/回测、运行记录及测试 | 指标/运算符/无未来函数固定样本、数据/策略版本记录、超时和错误分类测试 |
| FULL-402 | Android Kotlin DSL 解释器、资源限制、错误模型及 JVM 测试 | 节点语义、超时、取消、无数据、数值错误和输出限制测试；异常不崩溃 |
| FULL-403 | `tests/fixtures/` DSL 共享向量、两端向量加载器和一致性测试 | 同一向量分别运行 pytest 与 Android JVM；离散结果全等、浮点阈值边界测试 |
| FULL-404 | Android 策略参数/启停/历史/信号解释 UI、个人库关联及测试 | 参数校验、运行状态、持久化和 Compose 测试；设备审查触发条件、理由与风险标签 |
| FULL-500 | Android 个人库交易/费用/资金/持仓/策略归属实体、DAO、迁移与测试 | Room 迁移、约束、加密打开/重启和 CRUD 测试；行情库替换隔离回归 |
| FULL-501 | Android 交易录入/导入/修订、持仓计算与测试夹具 | 部分成交、多费用、出入金、拆分、撤销/修订和重复导入固定账本测试 |
| FULL-502 | 统计/归因领域逻辑、固定账本夹具、查询模型与测试 | 净值、胜率、盈亏比、最大回撤、暴露和多维归因逐项手算对照 |
| FULL-503 | 个人数据加密导出/恢复、密码/KDF、暂存回滚及测试 | 正确/错误密码、篡改/截断/旧版本、恢复中断测试；恢复前后逐表和统计一致 |
| FULL-504 | Android 交易复盘图表、筛选、备注、备份恢复流程 UI 及测试 | Compose/JVM 测试；真机完整执行录入、复盘、加密备份、清库恢复与错误回滚 |
| FULL-600 | 港股 Provider能力、标的范围、日历/复权规则、质量与测试 | 代表港股真实拉取、跨源重叠对比、日历/复权/尾部时段固定样本 |
| FULL-601 | 期货/商品指数/主力与连续合约模型、换月规则、聚合与测试 | 夜盘交易日、合约到期、换月、主力选择和连续价格固定样本及真实代表合约 |
| FULL-602 | 同花顺指数、ETF 去重、市场指标定义/采集/质量及测试 | 每项口径/单位/频率/来源/截止时间 Schema 测试；真实代表指标与去重样本 |
| FULL-610 | 独立 QMT 适配器、实时缓冲/断线恢复、能力报告与测试；禁止下单接口 | 模拟运行、断线/重连/重复 Tick/历史层隔离测试；用户账户环境实测后才可接受 |
| FULL-700 | 图谱实体/关系/证据/置信度/人工确认 Schema、领域模型与术语文档 | Schema 正反例、关系约束、证据定位和术语歧义固定样本；领域模型审查 |
| FULL-701 | HTML/Excel/PDF/公告导入器、原始定位/版本元数据与测试夹具 | 各格式固定样本、失败/重复导入、页码/单元格/DOM位置和解析版本追溯测试 |
| FULL-702 | 名称归一、实体消歧、关系抽取、合并逻辑、金标集与评估脚本 | 金标精度/召回阈值、别名/同名/冲突/重复样本；低置信结果进入待确认 |
| FULL-703 | 人工审核/修订/审计模型、桌面或 Android 审核接口及测试 | 自动/人工冲突、审计链、权限边界和并发修订测试；人工确认不可被自动覆盖 |
| FULL-704 | Android 图谱搜索、公司关系、证据与确认状态 UI/查询层及测试 | JVM/Compose 测试；设备从关系逐级追溯原始来源位置和确认状态 |
| FULL-800 | Windows 调度脚本、单实例锁、重试/通知、运行手册与测试 | 连续计划运行、重复触发、网络失败、受控断电/进程终止和恢复演练 |
| FULL-801 | 本地质量/运行历史/存储/来源健康看板、查询与测试 | 构造失败/陈旧/空间告警样本；从看板定位到来源运行、分区和质量问题 |
| FULL-802 | 签名/公钥轮换、脱敏、备份演练、依赖审计配置与文档 | 仓库/APK/日志敏感信息扫描，旧/新密钥包矩阵，备份恢复演练和依赖漏洞报告 |
| FULL-803 | 全模块必要回归修复、Release 构建配置、验收清单和报告 | `scripts/verify.ps1`、Release APK/16 KB、真实数据、真机、恢复、安全和性能全套验收 |
| FULL-804 | 云成本/授权/网络/存储测算与决策文档；不得购买或部署未批准资源 | 复核本机连续20次成功证据，年度总成本情景计算和用户书面批准/拒绝记录 |
| FULL-900 | 版本号、发布说明、签名 Release 产物、能力/质量/验收报告和标签；禁止新增功能 | 重跑 FULL-803 清单，校验 APK/报告哈希、版本/标签一致性和已知缺口完整性 |

## 7. 固定来源决策

- 历史行情优先验证 JQData；额度不足时比较 Tushare，仍不足才申请 RQData 试用。
- Tushare 优先承担交易日历、基础资料、财务和日频校验。
- AKShare 只按真实通过的单项能力启用，不允许来源级“一次成功全部启用”。
- BaoStock 定位为免费校验源；长期网络不可达不阻塞主链。
- QMT 在账户开通后承担个人实时行情，不进入云端。
- PTrade 不进入当前任务；系统不存在下单、订单账户或自动交易接口。
- TickDB.ai、TickFlow、FactorHub 缺少明确官方链接时保持 `BLOCKED`。

## 8. 每项代码审查标准

审查 Agent 按以下顺序输出带文件和行号的问题：

1. `P0`：凭据泄漏、个人数据损坏、错误交易日或无法恢复。
2. `P1`：数据错误、未来信息、静默换源、验收造假或公开契约破坏。
3. `P2`：失败处理、性能、迁移、并发和测试缺口。
4. `P3`：局部可维护性问题。

存在 `P0/P1` 时必须标记 `CHANGES_REQUIRED`。审查 Agent 不做无关重构，不得将单元测试通过视为真实数据验收。

## 9. 交付记录要求

每个任务在 `docs/deliveries/FULL-<编号>.md` 记录：任务与角色、结果、修改文件、实际测试命令与结果、真实来源或设备状态、公开接口/数据库/数据包迁移、安全与隐私影响、风险和未完成项、状态建议。审查与验收追加各自独立章节，不覆盖实现证据。

## 10. 外部门控与非阻塞支线

- 凭据、授权或设备缺失时，只阻塞依赖该条件的任务，不把整个计划伪装为失败或成功。
- `FULL-610` 仅在用户开通 QMT 后进入 `READY`。
- `FULL-804` 仅在本机生产任务连续 20 次成功后评估，任何付费资源必须用户单独批准。
- 离线正式版不依赖 QMT、云主机、RQData 试用或缺少官方链接的候选 API。

## 11. 固定启动指令

`FULL-001` 完成后，所有新任务必须先声明实现、审查或验收角色，并使用 `START_HERE.md` 中对应的固定提示词。实现角色领取 `READY` 或当前 `CHANGES_REQUIRED`，审查角色领取 `REVIEW`，验收角色领取 `ACCEPTANCE`；三类入口不得互相替代。
