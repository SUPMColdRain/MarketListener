# Copy this file outside the repository (for example, to %USERPROFILE%\market-monitor.env),
# fill in values locally, and pass it explicitly with: probe --config-file <path>.
# Never commit the copied file.
JQDATA_USERNAME=
JQDATA_PASSWORD=
TUSHARE_TOKEN=
