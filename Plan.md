# 行情监控和产业链图谱项目执行计划

## 0. 使用方法

本计划供 DeepSeek-V4-Flash-0731、ChatGPT 5.6 Luna 或其他执行 Agent 逐项实施。所有 Agent 开始前必须依次阅读：

1. `ADR.md`
2. `CONTEXT.md`
3. `行情监控和产业链图谱项目.md`
4. 本文件
5. `项目开发和Agent分工时序图.html`（用于理解协作顺序，不替代任务验收标准）

每次只领取一个任务编号。实现 Agent、审查 Agent、验收 Agent必须使用不同会话；架构不清楚时停止并提交问题，不得自行改变 ADR。

## 1. 统一交付格式

每个任务完成后必须提交：

- 任务编号和一句话结果。
- 修改文件列表。
- 实际执行的测试及结果摘要。
- 未完成项、外部阻塞和数据源真实状态。
- 是否改变数据库、数据包或公开接口；如改变，给出迁移说明。
- 不得提交账号、Token、签名私钥、个人交易数据和下载的全量行情。

审查 Agent 按“正确性 → 数据完整性 → 安全与隐私 → 性能 → 可维护性 → 测试缺口”的顺序给出发现。验收 Agent必须运行测试或操作程序，不得只阅读代码。

## 2. Day 0 成功标准

Day 0 使用少量真实代表标的跑通以下链路：

```text
数据源探测
  → 真实数据采集
  → 标准标的与标准 K 线
  → 数据质量报告
  → 交易时段聚合
  → 签名行情包
  → Android 13+ 导入
  → 本地 K 线展示
  → 签名 Python 策略包导入
  → 手机策略信号
  → 电脑与手机结果一致性报告
```

必须满足：

- 至少一个真实数据源从采集一直到手机策略信号全链成功。
- 聚宽、Baostock、AkShare、tdx_quant 均输出 `PASS`、`FAILED` 或 `UNSUPPORTED`，失败不伪装成成功。
- Android 在断网状态下能够导入、查询并运行策略。
- 行情包更新不会修改 `user.db`。
- 同一策略、参数和输入在电脑与手机产生相同离散信号；浮点值符合预设误差。
- QMT、云服务器、全市场下载、产业链图谱和完整交易复盘不属于 Day 0。

## 3. 阶段 A：仓库与环境基线

### D0-001 建立项目骨架

**执行**：创建 `desktop/`、`android/`、`contracts/`、`tests/fixtures/`、`docs/` 目录；增加根目录说明、忽略规则和示例配置。Python 使用 3.11 虚拟环境；Android 使用 JDK 21 LTS、Kotlin、Jetpack Compose，`minSdk=33`。

**产物**：电脑端可运行的空 CLI、Android 空应用、统一测试入口、无敏感值的 `.env.example`。

**测试**：Python 单元测试空套件运行成功；Android debug 构建和 JVM 单元测试成功。

**验收**：仓库中不存在真实凭据；执行命令和所需工具版本写入说明文档。

### D0-002 固化共享契约

**执行**：在 `contracts/` 定义并提供 JSON Schema：标准标的、K 线、数据源运行结果、质量问题、行情包清单、策略包清单和策略结果。契约版本从 `1` 开始。

**关键字段**：

- 标准标的键：`country_or_market`、`exchange`、`asset_type`、`code`。
- K 线：`instrument_key`、`period`、`trading_day`、`bar_open_time`、`bar_close_time`、`timezone`、OHLCV、`amount`、`open_interest`、`price_mode`、`source`、`source_period`、`quality_status`。
- 行情包：`package_id`、`schema_version`、`generated_at`、每个分区的 `data_cutoff`、文件大小、行数、SHA-256、来源运行摘要、最低 App 版本。
- 策略结果：策略/参数/数据版本、标的、信号时间、触发条件、观察理由、风险标签和运行状态。

**测试**：电脑端和 Android 端分别使用同一批合法与非法样例验证 Schema；非法时间、未知周期、缺失来源和非法 OHLC 必须被拒绝。

**验收**：业务代码不直接暴露聚宽或其他来源的专有代码格式。

## 4. 阶段 B：数据源能力探测

### D0-010 实现 Provider 接口与探针 CLI

**执行**：实现 `probe_capabilities`、`fetch_instruments`、`fetch_bars`、`fetch_indicators`、`fetch_calendar`、`health_check`。所有 Provider 返回统一运行结果，错误按认证、额度、网络、字段变化、无覆盖和未知错误分类。

**测试**：使用固定响应测试正常、空数据、超时、限流、字段缺失和部分返回；日志自动遮盖账号和 Token。

**验收**：单个 Provider 异常不会终止其他探针；进程最终生成机器可读和人可读两份能力报告。

### D0-011 聚宽真实探针

**执行**：凭据只从本机环境变量或系统凭据存储读取。探测两只 A 股、两只 ETF、一个普通指数、一个可发现的当前期货合约，优先检查 1d、30min 和最低可用分钟周期，范围为最近 30 个交易日。

**代表标的**：A 股使用 600519、000001；ETF 使用 510300、159915。来源代码由 Provider 映射，不写入业务层。指数和期货通过来源查询当前可用标的后选取并记录。

**验收**：报告真实额度、可用周期、返回行数、最早/最晚时间和错误；不得依据文档推断为通过。

### D0-012 Baostock 真实探针

**执行**：抓取与聚宽相同的两只 A 股，探测 1d、30min、复权因子和交易日历能力。

**验收**：生成跨源对比报告，比较日期覆盖、收盘价、成交量和复权口径；差异只报告，不自动逐行混合。

### D0-013 AkShare 真实探针

**执行**：按优先级探测 A 股涨跌家数、涨跌停统计、资金流向。首个成功指标进入 Day 0 数据包，其余继续报告能力状态。

**验收**：接口变化或反爬失败必须记录为 `FAILED`，不得用硬编码结果替代。

### D0-014 tdx_quant 真实探针

**执行**：先验证包名、安装来源、许可证、最近维护状态和可调用 API，再探测一只港股、一个可发现的指数和一个期货合约。文档声称的“同花顺 1293 个指数”只能在实际枚举、代码映射和样本拉取成功后标记为支持。

**验收**：无法安装、无明确 API、无法合法使用或覆盖不足时标记为 `UNSUPPORTED` 或 `FAILED`，并给出替代数据缺口，不阻塞其他来源闭环。

## 5. 阶段 C：电脑端标准化与存储

### D0-020 建立标准标的目录

**执行**：实现标准标的键和来源代码映射；股票、ETF、指数、期货合约和连续合约必须区分资产类型。标的范围保存规则版本、生效时间和成员有效期。

**测试**：同一 A 股的聚宽、Baostock 代码映射到同一标准键；港股补零、退市标的、期货换月和代码复用有固定测试。

### D0-021 建立 Bronze/Silver 数据层

**执行**：来源响应或规范化快照进入 Bronze；标准行情进入按市场、资产类型、周期和年份分区的 Parquet；DuckDB 保存目录、运行记录和查询视图。写入以分区为单位保持幂等。

**测试**：同一采集任务重复执行不会增加重复记录；失败重试不会覆盖上一个完整分区；时间统一使用带时区值。

### D0-022 实现数据质量规则

**执行**：检查主键重复、时间倒序、OHLC 关系、负成交量、缺失交易时段、异常跳变、来源截止时间和部分分区。问题分为 `INFO`、`WARNING`、`ERROR`、`QUARANTINED`。

**验收**：存在阻断错误的分区不能进入行情包；警告必须进入 `quality-report.json`。

### D0-023 实现交易时段聚合

**执行**：通过版本化交易时段规则生成 15min、30min、1h、2h、4h、1d；分别覆盖 A 股午休、港股时段、期货日盘与夜盘。保存原始不复权价格，复权因子独立存储。

**测试**：使用手工可核对样本验证 OHLCV、成交额和持仓量；禁止跨交易日合并，允许按规则产生尾部不足整周期的 bar，并明确标记。

## 6. 阶段 D：行情包生产

### D0-030 生成不可变行情包

**执行**：从 Silver 层选择代表样本，生成 `manifest.json`、`payload.sqlite`、`quality-report.json` 和签名文件。包名包含版本与生成时间，但包内身份以 `package_id` 为准。

**导入数据库**：只包含标准标的、来源映射、标准 K 线、指标、电脑端策略信号和元数据，不包含账号或个人数据。

**测试**：逐文件 SHA-256、清单 Schema、数据库行数、外键、唯一键和数据截止时间一致。

### D0-031 签名、校验与回滚样本

**执行**：生成开发期 Ed25519 密钥；私钥放在仓库外，公钥嵌入 Android。准备正常包、篡改包、旧 Schema 包、截断包和重复导入包。

**验收**：除正常包外，其余包必须以明确原因被拒绝；重复导入同一 `package_id` 不产生重复数据。

## 7. 阶段 E：Android 最小消费端

### D0-040 建立 Android 应用与数据库边界

**执行**：使用 Compose、Room、WorkManager、Android Keystore 和系统文件选择器。创建独立 `user.db`、版本化 `market_hot.db` 和冷数据目录；个人数据库使用 Keystore 包装的密钥保护。

**测试**：数据库创建、升级、应用重启和无网络启动；行情库删除后个人数据仍存在。

### D0-041 实现行情包原子导入

**执行**：选择文件后依次完成签名、哈希、Schema、空间和行数检查；解包到暂存目录，验证成功后原子切换活动版本。保留最近一个正常版本供回滚。

**测试**：覆盖篡改、空间不足、进程中断、重复包、降级包、未知 Schema 和旧 App 版本。

### D0-042 实现最小查询与 K 线页面

**执行**：提供导入历史、样本标的列表、数据截止时间、来源/质量状态和 K 线页面。K 线使用随 App 离线打包的 Lightweight Charts，通过受控 WebView 读取本地查询结果，不依赖公网资源。

**验收**：Android 13 真机断网可查看至少一个日线和一个分钟周期；页面明确显示数据截止时间和质量警告。

## 8. 阶段 F：Android Python 策略

### D0-050 验证嵌入式 Python

**执行**：使用 Chaquopy 建立独立策略进程，固定 Python 运行时、标准库和 NumPy 版本；禁止运行时 `pip install`。实现运行超时、取消、错误捕获和输出大小限制。

**验收**：Android 13 真机离线执行 NumPy 示例；超时脚本和异常脚本不会使主应用崩溃。若选定工具链无法打包 NumPy，停止任务并报告，不得私自改为联网执行。

### D0-051 实现策略包契约

**执行**：策略包包含 `manifest.json`、入口脚本、默认参数、测试向量和签名。统一入口：

```python
def run(bars, params, context):
    """Return a list of signal dictionaries matching the shared schema."""
```

策略只接收由 Kotlin 构造的行情与上下文，不获得数据库连接、Android Context 或数据源凭据。

**测试**：未知依赖、错误签名、重复版本、缺失字段和超时脚本均被拒绝或隔离。

### D0-052 实现首个一致性策略

**执行**：实现一个只依赖 NumPy 的版本化量价策略，在电脑端与手机端运行同一策略包、输入文件和参数。输出候选标的、触发条件、观察理由和风险标签。

**验收**：离散信号完全一致；浮点结果使用明确误差阈值；报告策略版本、参数版本、数据包版本和两端运行耗时。

## 9. 阶段 G：Day 0 审查与验收

### D0-060 独立代码审查

审查 Agent 重点检查：凭据泄露、个人库被覆盖、自然时间聚合、逐行混源、导入非原子、未签名脚本、Android 主线程重任务、错误被吞掉和测试只使用模拟数据。

所有高严重度问题必须修复并重新审查；中低严重度问题必须记录处理决定。

### D0-061 真机端到端验收

验收 Agent 在 Android 13+ 真机执行：

1. 电脑真实抓取并生成能力报告。
2. 生成并签名行情包与策略包。
3. 断开手机网络后导入两类包。
4. 查看 K 线、数据截止时间和质量报告。
5. 运行策略并与电脑结果比较。
6. 导入新行情包，确认个人数据不变。
7. 导入篡改包，确认拒绝且仍可使用旧版本。

### D0-062 Day 0 封板

只有 `D0-001` 至 `D0-061` 全部达到验收标准，Day 0 才能封板。封板产物包括版本标签、能力报告、质量报告、端到端验收报告、已知缺口和下一阶段建议。

## 10. Day 0 之后的阶段

1. 扩大 A 股与 ETF 范围，建立每日增量更新和标的范围版本。
2. 增加港股、期货、商品指数和更多市场指标。
3. 建立交易记录、资金曲线、归因和复盘模块。
4. 增加产业链图谱的结构化提取、人工抽查与 Android 查询。
5. 根据实测数据量确定热数据保留期和冷数据分片规则。
6. 可选迁移到廉价云主机：复用同一生产 CLI、数据包和签名流程，以定时任务加对象存储交付，不先建设在线业务后端。

## 11. 模型分工建议

**执行 Agent（DeepSeek-V4-Flash-0731 或 ChatGPT 5.6 Luna）**：领取单个任务，完成实现和自动化测试，不修改 ADR。

**审查 Agent（与执行者不同）**：只审查当前任务差异，优先找行为错误、数据错误、安全问题和缺失测试，不做无关重构。

**验收 Agent（独立会话）**：按任务验收标准运行命令、打开 App 或检查产物，保存结果摘要。

**高推理模型或用户**：只处理 ADR 变更、数据源重大替换、时间语义争议、策略安全边界和跨阶段方案调整。

任何模型如果无法访问真实账号、Android 真机或外部数据源，必须将任务标记为“环境阻塞”，不得用单元测试替代真实验收。

## 12. Day 0 阶段状态快照（2026-08-04）

本节是执行状态记录，不改变前述任务范围、架构决策或验收标准。

- 已实际通过：共享契约、桌面标准化/质量/行情包/签名代码及其单元测试（当前 49 passed）；Android 数据库边界、行情包校验代码、中文导入状态界面、已激活 payload 的读取/K 线转换代码、Android JVM 测试、Debug APK 构建、APK 16 KB 对齐检查和 16 KB 模拟器主界面启动。
- 数据源阻塞：聚宽缺少本机凭据；Baostock 在 20 秒受控探针中超时；AkShare 上游代理连接关闭；`tdx_quant` 无可安装的已验证发行包。因此尚无真实 Provider 生产的签名行情包。
- 非数据阻塞：Chaquopy 17.0.0 + Python 3.13 可离线打包 NumPy，但 16 KB 模拟器实际加载时 `chaquopy-libgfortran` 的 4096 对齐被拒绝；D0-050 按验收规则停止，D0-051 和 D0-052 未执行。D0-042 尚未用真实导入 bars 验收 K 线，D0-061 尚未完成端到端验收。
- 历史结论：D0-062 不满足封板条件。当时的后续入口为 [Day0阶段性交接](./docs/deliveries/Day0阶段性交接.md)、[Experience.md](./Experience.md) 和 [Log.md](./Log.md)，不得把自动化测试或空界面视为真实数据/真机验收。该“继续”指示已被第 13 节的用户停止决定取代。

## 13. Day 0 归档与正式开发转场（2026-08-04）

用户决定停止执行本文件中的 Day 0 任务。该决定不改变前述任务原文、验收标准、ADR 或历史测试结论：Day 0 未封板，所有未完成项继续保留为事实记录。

后续会话不得自动继续 `D0-*` 任务，也不得将本文件改写为整个项目的新开发计划。正式开发从 [docs/正式开发交接.md](./docs/正式开发交接.md) 开始，在用户确认项目级目标和验收标准后建立新的任务计划；复用 Day 0 代码或重新验证其阻塞项时，必须以新的任务和真实证据记录。
