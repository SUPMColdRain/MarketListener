# Agent 任务交付

## 实现交付

**任务编号**：D0-031

**结果**：实现 Ed25519 行情包签名/验签，生成开发期私钥到仓库外目录并嵌入 Android 公钥。

**修改文件**：`desktop/src/market_monitor/signing.py`、`desktop/tests/test_signing.py`、`android/app/src/main/res/raw/market_package_public_key.pem`、`desktop/pyproject.toml`。

**公开接口或数据变化**：新增开发密钥生成、包签名和验签接口；验签要求唯一签名、Schema 版本 1 和有效 Ed25519 签名。

**测试记录**：`desktop\\.venv\\Scripts\\python.exe -m pytest desktop\\tests -q`，PASS（48 项，包含正常、篡改、旧 Schema、截断包）。

**风险与未完成项**：Android 导入端验签和重复导入防护由 D0-041 实现；当前开发私钥为本机开发密钥，发布前需按发布流程轮换。

**自检**：

- [x] 私钥生成到 `%LOCALAPPDATA%\\MarketListener\\keys`，不在仓库。
- [x] Android 仅包含公钥。
- [x] 篡改、截断和旧 Schema 均拒绝。

## 代码审查交付

**审查任务**：D0-031

**结论**：APPROVE

**发现**：未发现阻塞问题；测试 Zip 的重复文件警告来自故意篡改样本，不影响验签拒绝结果。
