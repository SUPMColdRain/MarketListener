# Agent 任务交付

## 实现交付

**任务编号**：D0-020

**结果**：实现 SQLite 标准标的目录、来源代码生效期映射以及版本化标的范围和成员有效期查询。

**修改文件**：`desktop/src/market_monitor/catalog.py`、`desktop/tests/test_catalog.py`。

**公开接口或数据变化**：

- 新增 `InstrumentKey(country_or_market, exchange, asset_type, code)`，内部 ID 为四段标准键。
- 新增 `InstrumentCatalog` 的标的登记、来源映射解析、范围规则保存和范围成员解析接口。
- SQLite 新增 `instruments`、`source_mappings`、`universe_rules`、`universe_members` 表；来源映射与成员均有生效起止日期。

**测试记录**：

| 测试 | 实际命令或操作 | 结果 |
|---|---|---|
| 桌面单元测试 | `desktop\\.venv\\Scripts\\python.exe -m pytest desktop\\tests -q` | PASS（38 项） |
| 真源映射测试 | 未执行：外部 Provider 在当前环境均未取得可用行情 | 未执行 |

**数据源状态**：沿用阶段 B 实测状态：聚宽 `AUTH`、Baostock `NETWORK`、AkShare `NETWORK`、tdx_quant `UNSUPPORTED`。

**风险与未完成项**：当前映射仅由测试样本验证，尚未用成功的 Provider 响应建立真实映射记录；这不影响目录的时间语义和约束测试。

**自检**：

- [x] 同一 A 股跨来源归一。
- [x] 覆盖港股补零、退市、期货换月和代码复用。
- [x] 股票、ETF、指数、期货可通过 `asset_type` 区分。
- [x] 未将来源专有代码作为业务主键。
- [x] 未写入外部行情、凭据或个人数据。

## 代码审查交付

**审查任务**：D0-020

**结论**：APPROVE

**发现**：未发现阻塞问题。真实源映射验证受阶段 B 外部阻塞影响，已明确保留为测试缺口。
