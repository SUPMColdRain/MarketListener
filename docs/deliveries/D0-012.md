# Agent 任务交付

## 实现交付

**任务编号**：D0-012

**结果**：接入 Baostock 公共登录、两只 A 股 1d/30m、复权因子和交易日历探针，并实现聚宽与 Baostock 的只读跨源比较报告。

**修改文件**：`desktop/src/market_monitor/providers/baostock.py`、`providers/comparison.py`、`providers/joinquant.py`、CLI、注册表及相应测试。

**公开接口或数据变化**：新增 `BaostockProvider` 与 `market-monitor compare-sources`；比较报告固定 `row_blending: DISABLED`，只报告覆盖、收盘价、成交量和复权因子行数差异。

**测试记录**：

| 测试 | 实际命令或操作 | 结果 |
|---|---|---|
| 单元测试 | `desktop\\.venv\\Scripts\\python.exe -m pytest desktop\\tests -q` | PASS（35 项，执行时） |
| 真实 Baostock 探针 | `python -m market_monitor probe --report-dir artifacts\\d0-012-recheck` | FAILED / NETWORK |
| 真实跨源比较 | `python -m market_monitor compare-sources --report-dir artifacts\\d0-012-comparison` | BLOCKED；未混合数据 |

**数据源状态**：

| 来源 | 状态 | 样本与周期 | 行数/时间范围 | 失败或缺口 |
|---|---|---|---|---|
| 聚宽 | FAILED / AUTH | 无 | 0 | 缺本机凭据 |
| Baostock | FAILED / NETWORK | 无 | 0 | 实测服务连接失败，`WinError 10057` |

**风险与未完成项**：当前无法取得两源真实共同样本，故没有产生数值差异结论；跨源报告已明确为 `BLOCKED`，未自动逐行混合。

**自检**：

- [x] 固定响应只用于单元测试。
- [x] 真实报告记录认证、网络失败和比较阻塞。
- [x] 复权因子不再以资金流数据替代。
- [x] 不写入或合并任何来源行。

## 代码审查交付

**审查任务**：D0-012

**结论**：APPROVE

**发现**：未发现阻塞代码问题；外部网络与聚宽凭据阻塞真实对比，已在报告中保留。
