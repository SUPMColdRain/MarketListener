# Agent 任务交付

## 实现交付

**任务编号**：D0-010

**结果**：实现统一 Provider 接口、独立探测运行器、标准错误分类、凭据脱敏以及机器可读 JSON 和人可读 Markdown 能力报告。

**修改文件**：

- `desktop/src/market_monitor/providers/base.py`：六个 Provider 操作、统一结果和错误模型。
- `desktop/src/market_monitor/providers/runner.py`：异常隔离、脱敏、JSON/Markdown 双报告输出。
- `desktop/src/market_monitor/providers/__init__.py`：公共 Provider API。
- `desktop/src/market_monitor/cli.py`：`market-monitor probe --report-dir <dir>` 命令。
- `desktop/tests/test_provider_runner.py`：固定响应和报告回归测试。
- `contracts/provider-run-result.schema.json`：补充运行结果中的行数、时间范围和无覆盖错误类别。
- `.gitignore`：忽略本地能力报告目录。

**公开接口或数据变化**：

- 新增 `Provider` 抽象接口：`probe_capabilities`、`fetch_instruments`、`fetch_bars`、`fetch_indicators`、`fetch_calendar`、`health_check`。
- 新增 `ProviderRunResult`、`Capability`、`ProviderError` 和错误类别 `AUTH`、`RATE_LIMIT`、`NETWORK`、`PROVIDER`、`NO_COVERAGE`、`UNKNOWN`。
- 新增 `market-monitor probe --report-dir <dir>`，输出 `provider-capabilities.json` 与 `provider-capabilities.md`。

**测试记录**：

| 测试 | 实际命令或操作 | 结果 |
|---|---|---|
| 桌面单元测试 | `desktop\\.venv\\Scripts\\python.exe -m pytest desktop\\tests -v` | PASS（24 项） |
| CLI 报告输出 | `desktop\\.venv\\Scripts\\python.exe -m market_monitor probe --report-dir artifacts\\d0-010` | PASS（真实生成 JSON 和 Markdown） |
| Android JVM 测试 | 未执行：本任务未改动 Android 代码 | 未执行 |
| 真机测试 | 未执行：本任务为桌面端 Provider 基础设施 | 未执行 |

**数据源状态**：

| 来源 | 状态 | 样本与周期 | 行数/时间范围 | 失败或缺口 |
|---|---|---|---|---|
| 聚宽 | 未探测 | - | - | D0-011 待接入真实凭据和探针 |
| Baostock | 未探测 | - | - | D0-012 待接入真实探针 |
| AkShare | 未探测 | - | - | D0-013 待接入真实探针 |
| tdx_quant | 未探测 | - | - | D0-014 待核验合法 API 与安装来源 |

**风险与未完成项**：

- 当前 CLI 注册表为空，因此生成的报告如实说明没有注册 Provider；它不是任何数据源验收结果。
- 真实数据源访问、凭据、额度和反爬状态尚未测试，分别由 D0-011 至 D0-014 完成。
- Provider 实现必须把上游字段映射错误显式转换为 `PROVIDER`，不能静默忽略。

**自检**：

- [x] 已阅读 `ADR.md`、`CONTEXT.md` 和 D0-010 验收标准。
- [x] 未提交凭据、私钥、个人数据或大体积行情。
- [x] 固定响应仅用于单元测试，未冒充真实数据源探测。
- [x] 一个 Provider 失败后，后续 Provider 仍被执行并写入报告。
- [x] 错误消息中的账号、Token 和 Bearer Token 会自动脱敏。

## 代码审查交付

**审查任务**：D0-010

**结论**：APPROVE

**发现**：未发现阻塞问题。未覆盖的内容是四个真实 Provider 的运行状态和源字段兼容性，已作为 D0-011 至 D0-014 的明确范围。

**重点检查**：

- [x] 行为与任务验收标准一致。
- [x] 没有越过 ADR 约束。
- [x] 运行结果携带来源、时间、行数和覆盖范围字段。
- [x] 失败报告不会写入个人数据或覆盖行情版本。
- [x] 凭据不会进入报告或日志内容。
- [x] 覆盖正常、空数据、超时、限流、字段缺失、部分返回和 Provider 隔离。
- [x] 未将固定响应测试冒充真实数据源或真机验收。

## 2026-08-04 续执行记录

**实际完成**：为 `ProbeRunner` 增加每个 Provider 的受控超时，CLI 新增 `probe --timeout-seconds <seconds>`，确保一个上游调用挂起时其余来源仍会生成真实能力报告。

**修改文件**：

- `desktop/src/market_monitor/providers/runner.py`：每个 Provider 运行在线程中，超过上限后记录 `NETWORK` 失败并继续。
- `desktop/src/market_monitor/cli.py`：新增 `--timeout-seconds` 参数。
- `desktop/tests/test_provider_runner.py`：新增阻塞 Provider 不阻塞后续 Provider 的回归测试。

**公开接口或数据变化**：`market-monitor probe` 新增可选 `--timeout-seconds` 参数，默认值为 45 秒；既有 JSON/Markdown 报告格式不变。

**实际测试**：

| 测试 | 实际命令或操作 | 结果 |
|---|---|---|
| 桌面单元测试 | `desktop\.venv\Scripts\python.exe -m pytest desktop\tests -q` | PASS，49 passed |
| 全量真实 Provider 探针 | `desktop\.venv\Scripts\python.exe -m market_monitor probe --timeout-seconds 20 --report-dir artifacts\d0-resume-all-providers` | PASS：生成 JSON/Markdown 报告；所有来源如实为 FAILED/UNSUPPORTED |

**真实数据源状态**：

| 来源 | 状态 | 样本与周期 | 行数/时间范围 | 失败或缺口 |
|---|---|---|---|---|
| 聚宽 | FAILED | 无 | 无 | 缺少 `JQDATA_USERNAME` 与 `JQDATA_PASSWORD` |
| Baostock | FAILED | 无 | 无 | 20 秒探针超时，分类为 `NETWORK` |
| AkShare | FAILED | 无 | 无 | Eastmoney 上游经代理连接关闭，分类为 `NETWORK` |
| tdx_quant | UNSUPPORTED | 无 | 无 | 无可验证安装包或 API |

**风险与未完成项**：本次报告证明失败隔离和事实记录有效，但没有任何 Provider 达到 `PASS`，不能生成真实行情包或继续真实 Android 导入验收。

**自检**：

- [x] 超时结果来自实际等待 20 秒后的报告，不是模拟结果。
- [x] 未记录或输出聚宽凭据。
- [x] 未把 `FAILED` / `UNSUPPORTED` 表述为数据源已支持。
