---
status: accepted
---

# 数据源能力探测与分区级降级

聚宽、Baostock、AkShare 和 tdx_quant 的覆盖、额度和稳定性必须通过可重复探针确认，不能仅依据宣传、旧文档或模型记忆。数据源失败时允许项目继续生成有缺口的数据包，但必须按行情分区降级并在质量报告中公开失败，不得逐行混合多个来源掩盖问题。

## Consequences

数据源矩阵是运行产物而不是永久承诺；任何源可以被替换，但标准模型、数据包和业务代码不得依赖来源专有字段。
