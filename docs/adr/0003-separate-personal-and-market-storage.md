---
status: accepted
---

# 个人数据与行情数据分库存储

自选、交易历史、资金记录和策略参数存入独立的 `user.db`，导入的近期行情存入可替换的 `market_hot.db`，历史行情使用只读冷数据分片。该边界保证行情包更新、失败回滚和清理历史数据不会损坏不可重建的个人记录。

## Consequences

任何导入流程都必须先写入暂存版本、校验成功后原子切换；不得通过覆盖整个应用数据库来更新行情。
